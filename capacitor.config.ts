import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.jesusrebollar.agenda',
  appName: 'agenda',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
